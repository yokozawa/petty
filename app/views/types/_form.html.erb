<%= form_for @type do |f| %>
  <p><%= f.label :label %>:<%= f.text_field :label %></p>
    <% if @type.errors.any? %>
      <p class="error"><%= @type.errors.messages[:label][0] %><p>
    <% end %>
  <p><%= f.submit %></p>
<% end %>
